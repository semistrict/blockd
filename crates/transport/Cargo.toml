[package]
name = "blockd-transport"
version = "0.1.0"
edition.workspace = true
license.workspace = true
rust-version.workspace = true

[dependencies]
blockd-core = { path = "../core" }
tokio = { workspace = true, features = ["io-util", "macros", "rt"] }

[lints]
workspace = true
