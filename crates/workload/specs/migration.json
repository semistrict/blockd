{
  "schema": 1,
  "name": "migration",
  "seed": 2953629697,
  "shape": {
    "disk_volumes": 2,
    "pages_per_volume": 16,
    "backed_up": false
  },
  "phases": [
    { "op": "create" },
    {
      "op": "run",
      "operations": 200,
      "write_ppm": 750000,
      "sync_every": 50,
      "access": { "volumes": "disk", "hot": null }
    },
    { "op": "migrate", "to_host": 1 },
    {
      "op": "run",
      "operations": 200,
      "write_ppm": 750000,
      "sync_every": 50,
      "access": { "volumes": "disk", "hot": null }
    },
    { "op": "verify", "scope": "disk" }
  ]
}
