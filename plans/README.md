# Implementation Plans

Generated by the improve skill on 2026-08-17. Execute in the order below
unless dependencies say otherwise. Each executor must read the plan fully,
honor its STOP conditions, and update its row when done.
Completed plans move to `plans/archive/`; their identifiers and status rows
remain unchanged.

## Execution order and status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Parallelize independent production fault work without sharding actor state | P1 | M | — | REJECTED — superseded by benchmark-first Plan 002 |
| 002 | Profile realistic large-host hotspots before changing concurrency | P1 | L | — | IN PROGRESS — retained Lima runtime baseline complete; long/real-VM tiers remain |
| 003 | Restore fork-write correctness and runtime fault progress | P0 | M | 002 | DONE |
| 004 | Make independent volumes scale across CPU cores | P1 | M | 002, 003 | DONE |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) |
REJECTED (with one-line rationale)

## Execution order

- Continue Plan 002 on a suitable large host for the long-duration, real-VM,
  hardware-counter, NUMA, pressure, and lifecycle tiers. The retained Lima
  runtime matrix and instrumentation gate are complete; its evidence points to
  high-frequency whole-fleet observability scans before fault-work parallelism.
- Do not execute Plan 001. It is retained to preserve plan history and numbered
  identifiers.
- Plan 004 preserves the local actor and fork-sharing domain while overlapping
  only thread-safe kernel mapping work.

## Findings considered and rejected

- Implementing fault-work parallelism before measuring production-like
  workloads is rejected. The existing interference test proves degradation but
  does not identify whether actor polling, the fault-work queue, blocking-pool
  dispatch, kernel calls, storage, memory bandwidth, or another shared resource
  is responsible.
- Sharding `HostState` or `Cache` by volume is rejected as the first response to
  event-loop interference. Forks share base residency and memory mappings, and
  host pressure policy requires one authoritative cache view.
- Converting the protocol tree from `Rc<RefCell<_>>` to `Arc<Mutex<_>>` is
  rejected for this work. It would expand the correctness surface without
  addressing the already-visible one-at-a-time worker directly.
